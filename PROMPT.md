# デイリートレンド調査レポート 作成手順

毎日クラウドで自動実行される手順書。上から順に最後まで実行すること。
兄弟リポジトリ「AIニュース週報」（ai-news-weekly）と同じ流儀で作る日次版。**週報とジャンルを重複させない**のが最大の違い。

## 1. 日付を確定

★この手順書の日付はすべて日本時間（JST）で扱う。UTCを使わないこと（毎朝6時JSTの実行はUTCでは前日になり、日付が1日ズレて「本日分は作成済み」の誤判定を起こすため）。

`TZ=Asia/Tokyo date +%Y-%m-%d` で今日の日付を取得する。

★対象期間は**ジャンルごとに違う**（詳細は手順3・手順4）。ジャンルA（動画の作り方・演出）＝日付の縛りなし。ジャンルB（AIツール）＝直近7日以内。旧・丸1〜2日限定の一律縛りは廃止した（動画の作り方系の解説記事は公開日が常に古く、短い期間で切ると構造的に永遠に0件になっていたため。判断基準は「新しいか」ではなく「この読み手がまだ取り上げてへんか」に変えた＝手順2-0のseen.json参照）。

出力ファイル名は `days/<今日の日付>.html`（例：`days/2026-08-21.html`）になる。
※ネタが0件だった日は例外扱い。詳細は手順7を見ること。

## 2. 重複を避ける

### 2-0. seen.json（既出ネタの台帳）を読む ★必須

リポジトリのルート直下 `seen.json` を読む。**存在しなければ `{"urls": []}` として扱う**（新規作成前提。手順8-1で書く）。

`urls` 配列に載っているURLは、今回の候補から**問答無用で除外する**。

★**同じ記事が別URLで出てくることがある**（AMP版・短縮URL・転載）。URLが違っても**見出しと内容が実質同じなら重複として弾く**こと。

（ジャンルAは日付の縛りが無くなった分、何ヶ月前の記事でも再登場しうる。`days/` 直近5日だけでは追いきれないため、この台帳で全期間の既出を見る。以下の2-1・2-2はそのまま残し、seen.jsonはその上に**三重の網**として重ねる）

### 2-1. 自分の過去回と重複チェック
`days/` の中のHTMLファイル一覧を見て、**直近5日分を読む**。そこで既に取り上げたネタは今回は載せない。

ファイル数から今回が「第何回」かを決める（`days/` に既存が5ファイルなら今回は第6回。0件だった日はHTMLを作らないのでカウントに含まれない＝欠番扱いでよい）。

### 2-2. 週報と重複チェック（★このリポジトリ固有の工程）
**同じサンドボックスに `ai-news-weekly` リポジトリも一緒にクローンされている前提。** 実クローン先は実測で判明している。以下の順で探す：

1. 第一候補（固定パス）：`/home/user/ai-news-weekly`
2. 見つからなければ第二候補：`../ai-news-weekly`（作業ディレクトリの兄弟）

見つかったら、その `weeks/` の中のHTMLファイルのうち**直近2回分**を読む。週報で既に扱ったAIツールのニュースは、このレポートには載せない（AIツールの話題が週報と日報で二重に届くのを防ぐため）。

★ `ai-news-weekly` が見つからなかった場合は、この工程だけスキップして先に進む（他の手順は止めない）。ただし最終報告に「週報との重複チェックは未実施（リポジトリが見つからず）」と明記すること。

## 3. ネタを集める

### 読む人
美容室経営コンサルタント。エンジニアではないが、Claude Code / Notion / LINE Bot / 動画編集ツールを日常的に自分で触る実務家。**Instagramリールを自分で作っている**。

**制作の実際（採用判定に使う）**
- リールは**PC上で完結**して作る（ffmpegでの組み立て、AI画像生成、自作スクリプト）。**スマホの動画編集アプリの中で編集する工程が無い**。★スマホアプリ限定の新機能（アプリ内でしか使えない編集・自動組み立て等）は、「PCの作業に置き換えたら似た効果になる」という理屈で採らない。読み手はそのアプリを開かない
- 読み手自身の主な実務＝議事録・アンケート集計と分析・顧客管理・顧客向け資料とLP制作・Notion運用・LINE Bot運用・VPS運用・コラム執筆。ここに効く道具も採用対象
- 主力の「漫画リール」は**声が入ってへん**。文字を読ませる作りで、**BGMも動画に焼き込まん**（Instagram側の音楽ライブラリで後から付ける）。音声・BGM・声の編集に関する話は、下の「採用の条件」の追加ルールに従う
- ★**実写の自撮り動画はこれから始める段階**（撮影機材＝DJI Osmo Pocket 4P Vlogコンボを2026-09-02に発注済み・2026-09-04着予定。**機材が届いてもそれだけでは撮影開始にならない＝実際に撮り始めたとこの手順書に明記されるまでは、下の音声・BGM系の解禁条件は満たさない**）。始まれば声が入るので、音声まわりの話はその時点で価値が出る
- **既に日常的に使っとる**：Claude Code / Claude in Chrome / Notion / LINE Bot / VPS / ffmpeg。画像生成は下の内訳を見ること。**「正式に一般提供されました」系のニュースは、読み手が既に使っとる道具なら価値が無い**
- ★**画像生成の道具は用途ごとに固定されている**（2026-09-01確定）：note本文の写真＝Codex（`gpt-image-2`）／noteの見出し画像＝自作Pythonスクリプト`eyecatch.py`（AI生成ではなく文字を描画するだけ）／ゴリラ漫画＝ChatGPT（Claude in Chrome経由）／それ以外＝higgsfield（`soul_2`）。**Midjourney・Stable Diffusion・Adobe Firefly・Ideogram・DALL-E単体など、この4つに入らない画像生成ツールの新機能は、読み手がその場面で使う予定が無いので、それだけでは採用理由にならない**
- **使ってへん**：Google Chat、スマホの動画編集アプリ、Photoshop（2026-09-01に本人へ確認済み＝契約してへん）

**クライアントの実際（観点Aの企画論・撮り方論に関わる）**
- クライアントは美容室（複数店舗のグループを含む）
- ★**クライアントはInstagramリールをほとんど出してへん**。リールの企画論・分類フレームワーク・撮り方の話を渡しても、クライアントの現場では使い道が無い
- クライアントに刺さるのは：**物販・予約・顧客フォロー・組織や人材や採用**に関わるツール・仕組みの話

### 集める観点は2つだけ

**A. 動画の作り方・演出の新手法（3〜5本が目安）＝日付の縛りなし**
「冒頭フックの作り方」「テロップの当て方」「編集テンポ」「カット割りの新手口」「新しいAI動画ツールの具体的な使い方」など、**明日すぐ試せる作り方の話**。理論やなく手口を拾う。バズった動画の演出分析なども対象。
★この種の解説記事は「最新版」として常時更新される作りが多く、公開日は常に古い。**判断基準は「新しいか」ではなく「この読み手（手順2-0のseen.json参照）がまだ取り上げてへんか」**。古い記事でもseen.jsonに無ければ採用してよい。

**B. AIツールのニュース（0〜3本）＝直近7日以内**
新サービス・新機能の発表。ニュース性のあるジャンルなので鮮度は要る。★手順2-2で確認した週報の既出ネタとは重複させないこと。0本の日があってよい（無理に埋めない）。

### 集めないもの
株価・資金調達・買収・人事・規制や訴訟・研究論文・ベンチマーク比較・「AIが人類を〜」系の論説。**具体的な手口が書かれていない、理論だけの記事も落とす**（Aのジャンルは特に）。

### 採用の条件（★日付の縛りを外す分、ここで質を担保する）
- ★★**採る前に、「この読み手のどの作業に効くか」または「クライアントのどの場面で使えるか」を、上の『読む人』の実際に照らして具体的に1つ名指しで書けるか確かめる。書けんかったら、どれだけ新しくても具体的でも落とす。** 記事の内容が正しい・詳しいだけでは採用理由にならない
- ★**読み手が使ってへん／持ってへん道具（Google Chat／スマホの動画編集アプリ／Photoshop 等）の中でしか使えん機能は、どれだけ新しくても落とす。PC上の同等作業に置き換えたら効く、という理屈で採るな。** クライアントが使う場面を挙げるのも同様に禁止（例：「Photoshopで店販POPが作れる」は、読み手がPhotoshopを持ってへんので不採用）
- **具体的な手順・数字・実例が書かれているものだけ採る。** 「フックが大事」「テンポよく」のような抽象論だけの記事は、どれだけ新しくても落とす
- 同じ内容を言い換えただけの記事が大量にあるジャンルなので、**一番具体的に書いてある1本だけ**を選ぶ
- ★**読み手が「明日これを試せる」と言える形になっとるか**を最終判定にする。ならないものは落とす
- ★**音声・BGM・声の編集に関する話（EQ・ダッキング・ミキシング等の数値設定を含む）は、実写の自撮り動画の撮影が始まるまでは採らない。** 現状の主力（漫画リール）は無音・BGM後付けのため、編集する対象がそもそも存在しない。実写が始まったら（上の『制作の実際』を参照）解禁する。★**解禁は『制作の実際』の記述が更新されたときだけ。撮影が始まったかを自分で推測して解禁するな。**

### 調べ方
WebSearch を角度を変えて **最低4回以上** 回す（例：リール編集テクニック、冒頭フックの作り方、テロップ・字幕の当て方、バズった動画の演出分析、新しいAI動画/画像生成ツール、動画編集ツールの新機能アップデート）。**日本語と英語の両方**で検索する。

### 件数
AとB合わせて**3〜8件**が目安。重要度の低いものは自分で落とす。該当が少なければそれ未満でよい。
**絶対に水増ししない。架空のネタを作らない。**

## 4. 出典を実地確認する（最重要）

候補それぞれについて **WebFetch で実際にページを開き**、次を確認する：

- ページが実在して開けるか
- 中身が見出しと合っているか
- 具体的な手口・手順・数字・実例が本当に書かれているか（抽象論だけでないか＝手順3の採用の条件）
- ★**ジャンルBのみ**、公開日が直近7日以内か（ジャンルAは日付を見ない）

**開けないもの・内容が見出しと違うもの・具体性が無いもの・（ジャンルBで）日付が7日を超えるものは載せない。** 検索結果のタイトルだけで書かない。この工程は絶対に省くな。

## 5. HTMLを書く

まず `index.html`（このリポジトリのもの）を読み、**デザイン・CSS・カード構造をそのまま流用する**。見た目は毎日同じに保つこと。中身だけ差し替える。ブランドカラー：メイン `#D4722A` ／サブ `#C25F18` ／アクセント `#F5C28A` ／クリーム白基調（背景 `#FDFAF5`）。アクセント色は帯・バッジの背景にのみ使い、文字色には使わない（明るい背景で読めなくなるため）。

### 各記事に必要な要素
- ジャンルバッジ（A：動画の作り方／B：AIツール）＋重要度バッジ（高／中）
- 見出し（30字以内・日本語）
- **何があったか**：2〜3行。専門用語には（＝〜のこと）と短い説明を付ける
- **明日から試せること**：1〜2行。この読み手がすぐ真似できる／使えることを具体的に。**記事の要約ではなく「読み手の◯◯という作業が、こう変わる」の形で書く**（採用の条件で名指しした効き先をそのまま文章にする）
- 出典リンク（`target="_blank" rel="noopener"`）と日付

### ページ全体
- 冒頭に「第N回」を表記する。単一の対象期間は書かない（ジャンルA=縛りなし／ジャンルB=直近7日、と基準が違うため）。各記事の出典日付は個別に表示する
- 「今日の一言まとめ」を3行以内。関西弁まじりの話し言葉で、週報と同じトーンに合わせる
- 外部CDN・外部フォント・外部画像は一切使わない。CSSは全部インライン
- スマホで横スクロールが起きないこと

### 出力する2ファイル（掲載件数が1件以上のときのみ）
1. `days/YYYY-MM-DD.html` … 今日の日付。その日の保存版
2. `index.html` … 最新日の内容に更新。**末尾に `days/` 配下の過去回へのリンク一覧**を新しい順で付ける

## 6. latest.json を書く（LINE通知用・必須）

VPS側のスクリプトがこのファイルを読んでLINEに投げる。**掲載件数が0件でも必ず書く**（手順7参照）。ルート直下 `latest.json` に以下の形式で書く：

```json
{
  "date": "2026-08-21",
  "count": 4,
  "url": "https://nynyzeni-dot.github.io/daily-trend-radar/days/2026-08-21.html",
  "items": [
    {"title": "見出し", "why": "なんで拾ったか一行", "url": "出典URL"}
  ]
}
```

- `url` は0件の日は省略するかnullにする（対応する `days/*.html` が存在しないため）
- `items` の `why` は「明日から試せること」を一言に圧縮したもの。**記事の要約にせず、採用の条件で名指しした「読み手の◯◯という作業が、こう変わる」をそのまま圧縮する**

## 7. ネタが1件も無かった日の扱い（★通常の異常時処理と違うので注意）

`days/<日付>.html` は**作らない**。`index.html` も更新しない。だが `latest.json` だけは必ず以下の内容で書いてコミットする：

```json
{"date": "2026-08-21", "count": 0, "items": []}
```

**「今日はなし」と分かる形を必ず残すこと。** 何も出力しないと、壊れているのか本当にネタが無いだけなのか区別がつかなくなるため。

**0件扱いになるのは、実地確認（手順4）を通ったネタが本当に1件も無いときだけ。** 1件でも掲載可（Aジャンルは「明日試せる手口」なので1本でも成立する）。件数の多寡で非公開にする判断はしない。

## 8. コミットして push

### 8-1. seen.json を更新する（コミット前）

今回 `days/*.html` に掲載したネタのURL・見出し・日付を、`seen.json` の `urls` 配列に**追記**する（既存の中身は消さず追記のみ）。ファイルが無かった場合は `{"urls": []}` を新規作成した上で今回分を追記する。**0件の日は追記しない**（掲載が無いので台帳に足すものが無い）。

### 8-2. commit & push

`main` ブランチに直接 commit & push する。コミットメッセージは以下のいずれか：
- 掲載あり：`第N回（YYYY-MM-DD）を公開`
- 0件の日：`YYYY-MM-DD 該当なし（latest.jsonのみ更新）`

push が拒否された場合は、無理に別ブランチへ逃がさず、その旨をはっきり報告して終わる（手順9のD参照）。

## 9. 最後に死活のpingを打つ

**pingを打つかどうかは状況によって変わる。** 打ち漏らしは「壊れた」と誤検知される誤報になるし、打ちすぎると本当に壊れているのを隠す。以下の表に厳密に従うこと。

| 状況 | pingを打つか |
|---|---|
| 通常どおり公開できた | **打つ** |
| ネタ0件で `latest.json` だけ更新した | **打つ**（正常に「今日はなし」と判断できた＝仕組みは生きている） |
| 本日分が既に作成済みで何もしなかった（異常時B） | **打つ**（無害。二重起動だと分かるだけ） |
| push が拒否された（異常時D） | **打たない** |
| 途中でエラーになって手順を最後まで実行できなかった | **打たない** |

打つ場合のコマンド：

```
curl -fsS -m 10 --retry 3 "$HEALTHCHECK_PING_URL" || true
```

★ `$HEALTHCHECK_PING_URL` の実体は、**この手順書には書かれていない**。**routineの起動プロンプトで毎回渡される**ので、そこに書かれたURLを使うこと。なぜ手順書に書かないかというと、このリポジトリは公開されており、URLが漏れると第三者が偽のpingを打てて、壊れているのに監視が鳴らなくなるため。起動プロンプトにURLが見当たらない場合は、**pingを打たずにその旨を報告して終わる**こと（勝手にURLを探したり推測したりしない）。

## 10. 報告

最後に次を報告する：
- 掲載した件数と、各記事の見出し（0件の日は「該当なし」と明記）
- 落とした候補があれば、その理由（リンク切れ・中身が見出しと違う・具体性が無く抽象論だけ・ジャンルBで7日より古い・seen.json や過去回や週報と既出・読み手やクライアントへの効き先を名指しできず・読み手が持ってへん道具限定の機能・音声/BGM系で実写撮影が未開始のため保留 など）
- 週報との重複チェックを実施できたか（手順2-2）
- push が成功したかどうか

## 異常時の扱い（迷わずここに従う）

判断に迷ったら**何も壊さずに終わる**のが正解。中途半端に書き換えるより、何もせず報告するほうが良い。

### A. 拾えるネタが0件だった場合
手順7の通り。`days/` にはHTMLを作らず、`index.html` も変更しない。`latest.json` だけ `count:0` で更新してコミット・push（`seen.json`への追記は無し）。
「該当なし。理由（seen.json既出／過去回・週報と重複／出典が確認できず／採用の条件（手順3）を満たさず＝具体性不足・効き先を名指しできず・持ってへん道具限定・音声系で実写未開始／ジャンルBで7日超過／そもそも該当なし）」を報告して終わる。
**pingは打つ**（latest.json更新まで正常に完了しているため）。

### B. `days/<今日の日付>.html` が既に存在する、または `latest.json` の `date` が既に今日の場合
その日はもう作られている。**上書きは絶対にしない。** 連番（`-2` など）でファイルを増やすこともしない。何も変更せず、「本日分は作成済み」と報告して終わる。
**pingは打つ**（無害。二重起動だと分かるだけ）。

### C. 週報リポジトリ（ai-news-weekly）が見つからない場合
手順2-2の通りスキップして続行し、その旨を報告に含める。作業自体は止めない。

### D. push が拒否された場合
`claude/` 等の別ブランチに逃がさない。拒否された事実をそのまま報告して終わる。
**pingは打たない。**

## やってはいけないこと

- 出典を開かずに書く
- 件数合わせのための水増し
- 架空のネタ・架空のURL
- デザインを毎日変える
- 確認していない数字や機能を書く
- 週報と同じAIツールのネタを二重に載せる
